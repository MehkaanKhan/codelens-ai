# Phase 2 — Dataset Preparation (install locally)
datasets>=2.18.0
huggingface_hub>=0.21.0
python-dotenv>=1.0.0

# Phase 3 — QLoRA Fine-Tuning (install on Colab/Kaggle GPU only)
# transformers>=4.40.0
# peft>=0.10.0
# trl>=0.8.0
# bitsandbytes>=0.43.0
# accelerate>=0.28.0
